# Dependencies
node_modules

# Environment variables
.env

# Logs
logs/
*.log

# Build outputs (if needed later)
dist/
build/

# OS files
.DS_Store

# Package lock (optional - usually NOT ignored, so comment this out if needed)
# package-lock.json

# Firebase / config JSON (ONLY if sensitive)
# backend/*.json
